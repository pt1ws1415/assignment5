#include <lower_upper.h>
#include <stdio.h>

int main(int argc, char *argv[])
{
	if (argc < 2) {
		printf("Usage: %s string\n", argv[0]);
		return 1;
	}

	/********************************************************/
	/* 2. Implement a program, that tests both functions
	   from lower_upper.h on the first command line argument
	   passed. Print the altered string.
	*/

	/********************************************************/

	return 0;
}
